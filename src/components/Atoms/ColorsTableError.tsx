import { Alert } from '@mui/material';
import React from 'react';

export const ColorTableError = () => {
  return <Alert severity="error">Sorry, there are no colors avaliable to display yet.</Alert>;
};
